/// Patient feature barrel.
///
/// Owns the domain model for an active patient context and the helpers
/// for displaying it (PatientBanner widget lives in `widgets/`, the
/// design-system layer).
library;

export 'domain/patient_context_model.dart';
